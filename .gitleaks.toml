title = "HeartStone secret scanning"

[extend]
useDefault = true

[allowlist]
description = "Documented examples and deterministic test identifiers are not production secrets"
paths = [
  '''(?i)readme\.md$''',
  '''(?i)test/.*\.e2e-spec\.ts$''',
  '''(?i)\.env\.example$''',
]
